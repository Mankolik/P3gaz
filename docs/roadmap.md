Sprint 0: UI shell + canvas + camera + input + layers registry
Sprint 1: GeoJSON load (manifest) + draw polys/points + fitAll
Sprint 2: Traffic model + labels + FALT/QL/Filter OFF logic
Sprint 3: Strips + routes + conflict probe (horiz first)
Sprint 4: 5-pt calibration fallback (if needed), error report
